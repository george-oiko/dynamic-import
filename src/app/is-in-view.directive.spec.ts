import { IsInViewDirective } from './is-in-view.directive';

describe('IsInViewDirective', () => {
  it('should create an instance', () => {
    const directive = new IsInViewDirective();
    expect(directive).toBeTruthy();
  });
});
